[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py313"
exclude = ["alembic/versions", "models.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]
